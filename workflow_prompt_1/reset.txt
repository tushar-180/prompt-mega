RESET WORKFLOW, BDE DIGITAL PROSPECTING TEST HARNESS

Put the seven seeded prospect tasks in the ClickUp list named below back to the state the setup run left them in, so the Digital Prospect Audit & Outreach brief can be run again from scratch against the same queue. This is a reset run only. Don't research a company, don't open a prospect or competitor website, don't run PageSpeed, don't run a search on a prospect, don't create a task, and don't write or send an email. The only writing this run does is restoring seeded record values, removing what the master run created, and writing the reset report.

Every value this run reads or restores lives in the audit record block in the task description, not in a custom field. That block opens with the line [BDE AUDIT RECORD v1] and closes with the line [END BDE AUDIT RECORD], and between them sit twenty six keys, each one a line carrying the key name, a colon, and its value. Five of those keys hold lists instead, being Website Findings, Search Visibility Findings, PageSpeed Findings, Evidence / Sources and Competitors Reviewed, whose key line stops at the colon with each item on its own line below it opening with a hyphen and a space. Company Name means the ClickUp task name.

Clearing a key means leaving its line exactly where it is with nothing after the colon, and for a list key deleting the item lines beneath it. Never delete a key line, never delete a marker, and never rewrite the block from memory. Read the whole description, change only what you were told to change, write it back with every other character identical, and read it back afterwards to confirm all twenty six keys and both markers survived, because the ClickUp editor can reformat a description on save without saying so. A task that comes back with keys missing is worse than a task that was never reset, so fix it before moving on. Text above the opening marker or below the closing marker belongs to whoever typed it and is left alone.

This workspace runs on the ClickUp Free plan. Write to no custom field, since the plan's allowance is spent and the write fails; any custom fields still sitting on the List are inert, so leave them and do not try to clear them. Assign no comment to anyone, since assigned comments are not on the plan. Pace the work, since the plan allows a hundred API requests a minute and reading then rewriting a description counts twice, and wait out a rate limited call rather than reporting it blocked.

Report the run as blocked, naming the exact item and the exact error message, and stop, if any of these are unreachable or unauthorised: the ClickUp Space Sales, Folder Business Development, List Digital Audit Prospects, reached through the connected ClickUp MCP integration, or through the Chrome browser plugin where that integration errors; Gmail reached through the connected integration, or through the Chrome browser plugin where that integration errors, signed in to exactly one account whose address you name in the reset report; and the Chrome browser plugin where either integration errors and a step cannot be finished without it. Don't work around any of them. If Gmail offers more than one signed in account, stop and report every address it offers rather than choosing between them. If the List sits in a Space other than Sales because the Free plan's five Space cap forced it elsewhere, that is not a blocker: work on the List where it actually is and say so in the report.

Work only on the tasks in that one list whose Prospect Source key begins with SEED-A, SEED-B, SEED-C, SEED-D, SEED-E, SEED-F or SEED-G. Every other ClickUp Space, Folder and List is out of bounds. So is the Google Sheet BDE Service & Qualification Rules, so are the Drive folders under BDE Digital Prospecting, and so is every Gmail label except Drafts. Sent mail is read only, for the one check described below. Never delete a ClickUp task, never edit or delete a message that is not a draft, and never change a rule in the sheet.

Read the current state before changing anything, because this run erases the master run's output. Write into the reset report, for each of the seven tasks, its Company Name, Research Status, Review Cycle, Primary Opportunity, Recommended Service, Opportunity Score, Priority, Outreach Status, Next Action Date, whether a DIGITAL AUDIT SUMMARY comment for 2026-08 sits on it, and the subject line of any Gmail draft naming that company. That capture is the record of how the master run went, so it gets written before the first key is cleared.

Answer these six checks while the evidence is still there, each one as Pass or a single line describing what you found. BDE Notes on the SEED-A to SEED-G task carrying SEED-B still reads exactly: Met the owner at a trade expo, keep the first email short. Website URL on the SEED-D task is still empty. The list holds exactly seven tasks. The SEED-G task still carries Last Audit Date 2026-08-05 and Review Cycle 2026-08 and picked up no new comment. At most two Gmail drafts naming a seeded company sit in Drafts. And no mail went out to a seeded company on or after 2026-08-31 in Asia/Kolkata, checked by searching Sent for the seven company names, the seven domains, and the addresses contact@example.com, hello@example.net and contact@example.org. A failed check is the most useful thing this run produces, so write what you saw rather than smoothing it over.

If the list holds more than seven tasks, name the extra ones in the report and leave them alone. Deleting a task is out of bounds here, and an extra task is evidence the master run broke its own rule, so a person decides what happens to it. Say plainly in the report that an extra task with Research Status Pending will enter the next master run's queue until someone clears it.

Clear these fifteen keys on all seven tasks, whatever they currently hold: Contact Name, Contact Role, Contact Source, Website Findings, Search Visibility Findings, PageSpeed Findings, Evidence / Sources, Competitors Reviewed, Primary Opportunity, Recommended Service, Opportunity Score, Priority, Decision Maker, Next Action and Next Action Date. Cleared means the key line stays and its value goes, with the item lines under the five list keys deleted. Leave the task name, Website URL, Company Domain and Prospect Source exactly as they are, except where the lines below say otherwise.

Then set the seven tasks back to these values, one line per task.

SEED-A. Research Status Pending. Outreach Status Not Started. Re-Audit Required No. Last Audit Date empty. Last Outreach Date empty. Review Cycle empty. Contact Email contact@example.com. BDE Notes empty.

SEED-B. Research Status Pending. Outreach Status Not Started. Re-Audit Required No. Last Audit Date empty. Last Outreach Date empty. Review Cycle empty. Contact Email hello@example.net. BDE Notes reads exactly this line and nothing more: Met the owner at a trade expo, keep the first email short. If it reads anything else, put that exact line back and record what it held before you did.

SEED-C. Research Status Pending. Outreach Status Not Started. Re-Audit Required No. Last Audit Date empty. Last Outreach Date empty. Review Cycle empty. Contact Email empty. BDE Notes empty.

SEED-D. Research Status Pending. Outreach Status Not Started. Re-Audit Required No. Last Audit Date empty. Last Outreach Date empty. Review Cycle empty. Contact Email empty. BDE Notes empty. Website URL empty and Company Domain empty. If either one now holds a value, clear it and record that the master run filled in a website it was told never to guess.

SEED-E. Research Status Pending. Outreach Status Not Started. Re-Audit Required No. Last Audit Date 2026-06-20. Last Outreach Date 2026-07-10. Review Cycle 2026-06. Contact Email contact@example.org. BDE Notes empty.

SEED-F. Research Status Pending. Outreach Status Not Started. Re-Audit Required Yes. Last Audit Date 2026-07-15. Last Outreach Date empty. Review Cycle 2026-07. Contact Email contact@example.com. BDE Notes empty.

SEED-G. Research Status Completed. Outreach Status Not Started. Re-Audit Required No. Last Audit Date 2026-08-05. Last Outreach Date empty. Review Cycle 2026-08. Contact Email empty. BDE Notes empty.

Delete from those seven tasks every comment the workflow posted on or after 2026-08-31 in Asia/Kolkata, which covers each DIGITAL AUDIT SUMMARY comment the master run wrote and each Website URL required comment. Two comments stay, and nothing else gets deleted: the DIGITAL AUDIT SUMMARY comment on SEED-F reading Seed record for cycle 2026-07, not a real audit, and the one on SEED-G reading Seed record for cycle 2026-08, not a real audit. A comment a person typed stays where it is whatever its date. If you cannot tell whether a comment came from the master run or from a person, leave it and name it in the report.

Search the Drafts folder of that one signed in account for drafts created on or after 2026-08-31 in Asia/Kolkata carrying one of the seven seeded company names, one of the seven seeded domains, or one of the addresses contact@example.com, hello@example.net and contact@example.org in the To line, the subject or the body. Record each one's subject line and recipient in the report, then discard it. A draft matching none of that stays untouched, and so does everything outside Drafts. Discard nothing you are unsure about. Leave it, and name it in the report so a person can look.

Run this reset twice and the second run shouldn't change a thing: no field written, no comment deleted, no draft discarded. Compare each field against the value it should hold and write only where the two differ. Report the second run as already reset, with the same seven line capture showing seed values.

Do every piece of ClickUp work through the connected ClickUp MCP integration first, which covers reading a task description, rewriting one, and deleting a comment. Reach for the Chrome browser plugin on a ClickUp step only where that MCP is unavailable, errors, refuses the call, or hands back something that does not match what the ClickUp screen itself shows, and name in the reset report every ClickUp step that went through the browser rather than the MCP. Use the Chrome browser plugin wherever a direct integration cannot do the job or returns something unclear, which covers rewriting a task description, deleting a ClickUp comment, discarding a Gmail draft, and reaching the ClickUp or Gmail interface when an integration errors or silently does nothing. Mark a step blocked, rather than assuming it worked, if the plugin is unavailable and the step needs it.

Treat the run as done when all seven tasks hold the values listed above, each still carrying both markers and all twenty six keys; when the only DIGITAL AUDIT SUMMARY comments left in the list are the two seed lines on SEED-F and SEED-G; when no draft naming a seeded company sits in Drafts; when no task was created or deleted; and when the reset report carries the before capture of all seven tasks, the six checks with their answers, a list of what was cleared, deleted and discarded, and anything left behind for a person to look at. Close with one line confirming that a fresh master run dated 2026-08-31 would again select SEED-A, SEED-B and SEED-C.
