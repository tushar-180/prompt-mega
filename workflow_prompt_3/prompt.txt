Open Google Drive first and open the **Client Project Folder** named `ABC Retail - E-Commerce Platform`. Inside this folder, use the exact project documents named `ABC Retail - Final SOW.pdf`, `ABC Retail - Project Estimate.xlsx`, `ABC Retail - Approved Change Requests.docx`, and `ABC Retail - Project Plan.docx`. Treat `ABC Retail - E-Commerce Platform` as the canonical **Project Name** and `ABC Retail` as the canonical **Client Name** throughout the workflow.

Open Google Sheets and open the **Project Effort Tracker** named `ABC Retail - Development Effort Tracker`. Use this exact Sheet name when comparing estimated hours, actual hours, billable hours, non-billable hours, overtime, and recorded effort. Do not create another effort sheet if `ABC Retail - Development Effort Tracker` already exists.

Open Jira and open the **Jira Project** named `ABC Retail E-Commerce` with the **Jira Project Key** `ABCR`. Use `ABCR` whenever referring to Jira issues. Review the **Jira Epic** records, **Jira Story** records, **Jira Task** records, **Jira Bug** records, and the **Jira Project Closure Issue** `ABCR-150`. Use `ABCR-150` as the main Jira issue for recording the final project closure status and handoff information.

Open GitHub and open the **GitHub Web Repository** named `abc-retail-web` and the **GitHub API Repository** named `abc-retail-api`. Use `abc-retail-web` for frontend delivery evidence and `abc-retail-api` for backend delivery evidence. Review the Pull Requests, commits, branches, releases, and merged changes that correspond to the `ABC Retail - E-Commerce Platform` project. Do not use another repository unless the project records explicitly identify it as part of this project.

Open Slack and open the **Slack Project Channel** named `#abc-retail-project`. Review project discussions, scope changes, client requests, approvals, technical concerns, deferred work, delivery risks, and future requirements recorded in this channel. Use the exact channel name `#abc-retail-project` whenever referring to the project communication source.

Open Gmail and search for the **Client Email Thread** with the subject `ABC Retail - Project Closure`. Review the email thread for formal scope changes, client approvals, unresolved requirements, delivery concerns, deferred requests, client feedback, and future requirements. Also review related email conversations containing the exact client name `ABC Retail` and project name `ABC Retail - E-Commerce Platform` when additional evidence is required.

Open Google Calendar and review the **Project Closure Meeting** named `ABC Retail - Project Closure Meeting`. Review the meeting details, attendees, notes, agenda, and any linked project documents to identify final delivery decisions, unresolved items, client concerns, and future requirements.

Open Notion and open the **Notion Project Knowledge Database** named `Client Projects`. Inside that database, open the **Notion Project Knowledge Page** named `ABC Retail - E-Commerce Platform`. Update the existing project knowledge record with the final delivery status, deferred work, lessons learned, recurring client pain points, technical decisions, and evidence-backed future opportunities. Do not create a duplicate project knowledge page when `ABC Retail - E-Commerce Platform` already exists.

Open HubSpot and open the **HubSpot Company Record** named `ABC Retail`. Review the existing company information and open the **HubSpot Expansion Deal** named `ABC Retail - Mobile Application Expansion` when the evidence confirms that the opportunity is mature enough for Sales follow-up. Do not create a new deal for an unsupported or speculative opportunity. Do not enter pricing, commercial commitments, proposal values, or promised delivery dates.

Use the following canonical output document names throughout the workflow. Create the **Technical Closure Document** as `ABC Retail - E-Commerce Platform - Technical Closure.docx`. Create the **Project Closure Document** as `ABC Retail - E-Commerce Platform - Project Closure.docx`. Create the **Delivery Variance and Lessons Learned Document** as `ABC Retail - E-Commerce Platform - Delivery Variance and Lessons Learned.docx`. Create the **Sales Expansion Opportunity Brief** as `ABC Retail - E-Commerce Platform - Sales Expansion Opportunity Brief.docx`. Create the **Technical Estimation Plan** as `ABC Retail - E-Commerce Platform - Technical Estimation Plan.docx`.

Create a Gmail draft with the exact subject `ABC Retail - E-Commerce Platform - Sales Handoff`. The draft must summarize the completed project, unresolved items, deferred requirements, evidence-backed expansion opportunities, and technical estimation status. Do not send this email. The workflow must only prepare the draft for human review.

Use the following naming convention consistently across the entire workflow. The **Client Name** is `ABC Retail`. The **Project Name** is `ABC Retail - E-Commerce Platform`. The **Client Project Folder** is `ABC Retail - E-Commerce Platform`. The **Project SOW Document** is `ABC Retail - Final SOW.pdf`. The **Project Estimate Document** is `ABC Retail - Project Estimate.xlsx`. The **Approved Change Requests Document** is `ABC Retail - Approved Change Requests.docx`. The **Project Plan Document** is `ABC Retail - Project Plan.docx`. The **Project Effort Tracker** is `ABC Retail - Development Effort Tracker`. The **Jira Project** is `ABC Retail E-Commerce`. The **Jira Project Key** is `ABCR`. The **Jira Project Closure Issue** is `ABCR-150`. The **GitHub Web Repository** is `abc-retail-web`. The **GitHub API Repository** is `abc-retail-api`. The **Slack Project Channel** is `#abc-retail-project`. The **Client Email Thread** is `ABC Retail - Project Closure`. The **Project Closure Meeting** is `ABC Retail - Project Closure Meeting`. The **Notion Project Knowledge Database** is `Client Projects`. The **Notion Project Knowledge Page** is `ABC Retail - E-Commerce Platform`. The **HubSpot Company Record** is `ABC Retail`. The **HubSpot Expansion Deal** is `ABC Retail - Mobile Application Expansion`. The **Sales Handoff Gmail Draft** is `ABC Retail - E-Commerce Platform - Sales Handoff`.

Never replace these names with vague references such as “the project folder”, “the relevant sheet”, “the Jira space”, “the repository”, “the Slack channel”, “the client document”, “the related deal”, or “the appropriate record”. Always identify the resource using its defined resource type and exact resource name. For example, say **Project Effort Tracker: `ABC Retail - Development Effort Tracker`**, not “the effort sheet”. Say **Jira Project: `ABC Retail E-Commerce` with Project Key `ABCR`**, not “the Jira space”. Say **GitHub Web Repository: `abc-retail-web`**, not “the frontend repository”. Say **Notion Project Knowledge Page: `ABC Retail - E-Commerce Platform`**, not “the Notion page”.

If direct access to Google Drive, Google Sheets, Jira, GitHub, Slack, Gmail, Google Calendar, Notion, or HubSpot fails, use the **Chrome Browser Fallback** to open the same resource by its exact canonical name. Do not search randomly for similarly named resources. Do not create a replacement resource simply because the direct application connection failed.

All references across the workflow must use the same canonical names. If a resource is updated, the update must be made to the existing resource with the exact name defined above. The workflow must not create duplicate Sheets, Jira projects, GitHub repositories, Slack channels, Notion pages, HubSpot companies, HubSpot deals, or project documents when the canonical resource already exists.
