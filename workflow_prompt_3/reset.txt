RESET WORKFLOW, ABC RETAIL PROJECT CLOSURE AND SALES HANDOFF SYSTEM

I run this after a master run of my project closure and sales handoff workflow, so the workspace goes back to the exact state my setup run left it in and I can test the master prompt again against the same starting conditions.

This is a RESET RUN ONLY.

Do not classify any scope item.
Do not calculate any variance figure.
Do not rank or qualify any expansion opportunity.
Do not write any closure document.
Do not create any Jira issue or any Gmail draft.
Do not send any mail.
Do not rebuild the Drive folder, the 4 source files, the effort tracker, the seeded Jira issues, the repositories, the Slack channel, the seeded Gmail threads or the calendar event. That is the setup run's job, and this run only undoes what the master run wrote.
Do not delete anything that is not named in this brief.

All dates here use Asia/Kolkata and are written as YYYY-MM-DD.


WHY MOVING ABCR-150 BACK TO IN PROGRESS IS NOT ENOUGH ON ITS OWN

The master workflow searches for its own earlier output before it writes anything, so it updates whatever it finds instead of creating it fresh. Leave the 3 documents, the Variance Summary tab, the carryover issues, the closure comment and the Gmail draft in place, and the second test run takes the update path rather than the create path. That is a different test from the one I want to repeat.

Putting ABCR-150 back to In Progress only fixes the status. Everything below is what makes the second run a genuine repeat of the first.


BEFORE YOU START, CONNECTIONS AND TOOLS

Confirm you can reach Google Drive, Google Sheets, Jira Cloud and Gmail. I stay signed in to all of them in Chrome, so the browser route is open for every one of them.

Nothing here is called missing, unavailable, unauthorised or blocked until the Chrome browser plugin has been pointed at it and failed too. Where an integration is absent, errors, times out, refuses the call, reports a tool it does not recognise, or hands back nothing while the screen is plainly showing something, do that same piece of work through the Chrome browser plugin and say in the reset report which pieces went that way and what the error was.

This applies to deleting and editing, not just reading. If the Jira connector will not delete an issue, delete it in Chrome. If the Sheets connector will not drop a tab, drop it in Chrome. If the Gmail connector will not delete a draft, delete it in Chrome.

A connector that was never set up, a permission refusal, a message saying there is no tool for that job, a plugin that will not load, or a page that hangs are all reasons to open the app in Chrome and keep going, never reasons to stop. Do not ask me to connect an integration mid run either, the browser route is already there. Do not finish this run telling me an app is unavailable or a plugin is not working unless the report also carries what happened when you opened that same app in Chrome.

If something genuinely cannot be undone on either route, mark that piece blocked, quote the exact error text from both routes, and carry on with everything else.

Run this twice and the second run should find nothing left to undo and say so. An item that is already gone is not an error, it is the expected result. Report it as already clean and move on.


PART 1, GOOGLE DRIVE

Open the folder ABC Retail Ecommerce Platform in My Drive.

Exactly 5 things belong in that folder and nothing else:

ABC Retail Final SOW.pdf
ABC Retail Project Estimate.xlsx
ABC Retail Approved Change Requests.docx
ABC Retail Project Plan.docx
ABC Retail Development Effort Tracker

Delete everything else in that folder, then empty those items out of Drive Trash so a later search cannot find them. The master run should have left 3 Google Docs behind, named ABC Retail Ecommerce Platform Project Closure 2026-09-02, ABC Retail Ecommerce Platform Delivery Variance and Lessons Learned 2026-09-02 and ABC Retail Ecommerce Platform Sales Expansion Opportunity Brief 2026-09-02.

Do not rely on those exact names. If the master run named something differently, it is still not on the keep list of 5, so it still goes. Work off the keep list, not off the expected names, and tell me in the report every filename you actually deleted.

Leave the 4 source files completely alone. Do not open them for editing, do not rename them, do not re-export them.


PART 2, THE EFFORT TRACKER

Open the Google Sheet ABC Retail Development Effort Tracker in that same folder.

Delete the tab named Variance Summary. If the master run named its output tab something else, delete that too. Exactly 1 tab called Effort Log should remain when you are done.

The Effort Log tab does not get touched at all. Confirm before you finish that it still holds row 1 headers reading Module, Estimated Hours, Actual Hours, Billable Hours, Non Billable Hours, Overtime Hours, Rework Hours, Defect Reference, Developer, and 6 data rows from row 2 down, with Admin Dashboard still showing 0 in Estimated Hours. If any of that has been altered, do not repair it yourself, tell me in the report exactly what changed so I can rebuild the sheet from the setup run.


PART 3, JIRA

Open the project ABC Retail Ecommerce, key ABCR.

Delete every issue carrying the label carryover-2026-09-02. There should be around 4 of them, raised against the deferred and disputed scope items.

Then check for strays. These 15 keys are the only issues the setup run created: ABCR-1, ABCR-2, ABCR-10, ABCR-12, ABCR-14, ABCR-16, ABCR-20, ABCR-22, ABCR-24, ABCR-26, ABCR-28, ABCR-30, ABCR-38, ABCR-40, ABCR-150. Any other issue in ABCR came from the master run, whether or not it got the label, so delete it as well. If the setup run reported different real keys because the site would not let it control numbering, use that mapping instead of this list and say so in the report.

On ABCR-150:

Delete every comment on it. The setup run left it with no comments at all, so anything there now came from the master run.
Move its status from Closed back to In Progress.
Clear its resolution if one got set.
Remove any link the master run added to a carryover issue, if deleting those issues did not already clear the links.
Leave its summary, description and epic exactly as they are.

Do not change the status or resolution of any other issue. ABCR-22, ABCR-24 and ABCR-26 stay as they are, To Do, To Do and In Progress. Everything else that reads Done stays Done.


PART 4, GMAIL

Delete the draft with the subject ABC Retail Ecommerce Platform Sales Handoff, and any other draft carrying ABC Retail in its subject. Empty them out of Trash as well, because the master run searches Drafts by subject before it writes.

Leave the 2 seeded threads exactly as they are. The thread ABC Retail Project Closure keeps its 3 messages and the thread ABC Retail Invoice Query keeps its single message. Do not delete, archive, mark or reply to either of them.


PART 5, WHAT DOES NOT GET TOUCHED AT ALL

Nothing in this run goes near any of these, and none of them should show a change when you are finished:

The 4 source documents in the Drive folder.
The Effort Log tab.
The 15 seeded Jira issues, other than the status, resolution, comments and links on ABCR-150.
The repositories abc-retail-web and abc-retail-api, including the merged pull requests, the 3 unmerged ones and the releases.
The Slack channel abc-retail-project and every message and thread reply in it.
The 2 seeded Gmail threads.
The calendar event ABC Retail Project Closure Meeting on 2026-08-28.


PART 6, CHECK YOUR OWN WORK

Open each of these and confirm what you see, do not assume:

The Drive folder holds exactly the 5 keep list items and Drive Trash holds none of the deleted documents.
The tracker sheet shows 1 tab named Effort Log with its 6 data rows intact.
A search of ABCR for the label carryover-2026-09-02 comes back empty, ABCR-150 reads In Progress with no comments and no resolution, and the issue count in ABCR matches the 15 seeded keys.
A Gmail search for ABC Retail in Drafts comes back empty, and both seeded threads still open normally.


RESET REPORT

Finish by telling me, in plain text:

Every filename deleted from the Drive folder, and whether Trash was emptied.
Whether the Variance Summary tab existed and was removed, and confirmation that Effort Log is unchanged, or exactly what changed on it if something did.
Every Jira issue key deleted, the number of comments removed from ABCR-150, and the status and resolution ABCR-150 now reads.
Every Gmail draft deleted.
Anything the master run left behind that this brief does not cover, described but not deleted, so I can decide what to do with it.
Anything that was already clean before you started, so I know the master run never got that far.
Every step that fell back to the Chrome browser plugin, with the error that sent it there.
Every step still blocked, with the exact error text from both routes.
