RESET WORKFLOW, EMERGENCY RECOVERY ROUTE READINESS SYSTEM

I run this after a master run of my emergency recovery route readiness and release slot allocation workflow, so the workspace goes back to the exact state my setup run left it in and I can test the master prompt again against the same starting conditions.

This is a RESET RUN ONLY.

Do not classify any incident.
Do not work out any recovery route or recovery readiness status.
Do not allocate any release window.
Do not create any Gmail draft, any GitHub issue or any Jira issue.
Do not send any mail.
Do not rebuild the Jira project, the 8 seeded issues, the 5 repositories with their branches, pull requests, releases and workflow runs, the Drive folder, or the tracker sheet with its Release Windows and Recovery Contacts rows. That is the setup run's job, and this run only undoes what the master run wrote.
Do not delete anything that is not named in this brief.

All dates here use Asia/Kolkata and are written as YYYY-MM-DD.


WHAT THE MASTER RUN ACTUALLY WROTE, SO THIS IS SHORT

The master run only writes in 3 places. It leaves 1 comment on each of the 6 in scope Jira records, it writes an Incident ID into the Existing Allocation cell on the 2 windows it took, and it leaves 1 unsent draft in Gmail. Undoing those 3 things is the whole job here, and everything else in this brief is a check that the master run stayed inside its lane.

There is a reason the comments have to go rather than just being left sitting there. The master run searches for its own earlier output before it writes anything, so it updates whatever it finds instead of creating it fresh. Leave the comments, the allocation cells or the draft in place and the second test run takes the update path rather than the create path, which is a different test from the one I want to repeat.

The master run is not allowed to edit a Jira description, move an issue status, touch the Recovery Contacts tab, add a tab, write to GitHub or send anything. Every one of those is checked below, and anything you find is a rule that run broke, so name it in the report rather than quietly tidying it away.

Run this twice and the second run should find nothing left to undo and say so. An item that is already gone is not an error, it is the expected result. Report it as already clean and move on.


BEFORE YOU START, CONNECTIONS AND TOOLS

Confirm you can reach Jira Cloud, GitHub, Google Sheets, Gmail and the Chrome browser plugin. I stay signed in to all of them in Chrome, so the browser route is open for every one of them.

Nothing here is called missing, unavailable, unauthorised or blocked until the Chrome browser plugin has been pointed at it and failed too. Where an integration is absent, errors, times out, refuses the call, reports a tool it does not recognise, or hands back nothing while the screen is plainly showing something, do that same piece of work through the Chrome browser plugin and say in the reset report which pieces went that way and what the error was.

This applies to deleting and editing, not just reading. If the Jira connector will not delete a comment, delete it in Chrome. If Sheets will not clear a cell or drop a tab, do it in Chrome. If GitHub will not delete an issue, delete it in Chrome. If Gmail will not delete a draft, delete it in Chrome.

A connector that was never set up, a permission refusal, a message saying there is no tool for that job, a plugin that will not load, or a page that hangs are all reasons to open the app in Chrome and keep going, never reasons to stop. Do not ask me to connect an integration mid run either, the browser route is already there.

Do not finish this run telling me an app is unavailable or a plugin is not working unless the reset report also carries what happened when you opened that same app in Chrome. A piece reported as blocked without the browser route having been tried is a failed reset, and I will find out on the next master run rather than now.

If something genuinely cannot be undone on either route, mark that piece blocked, quote the exact error text from both routes, and carry on with everything else.


PART 1, JIRA

Open the project Incident Recovery Management, key IRM.

The setup run created 8 issues and no more, IRM-1 through IRM-8, or whatever real keys the setup report recorded if the site would not let it control numbering. Any other issue sitting in IRM came from the master run, so delete it and name it in the report.

Delete every comment on every issue in the project, with 1 exception. Every comment the master run left opens with the line Recovery readiness review 2026-09-03, so those are the obvious ones, but take the others too, because the only comment that stays is this. IRM-3 keeps the comment reading Testing complete, all green, ready to ship. QA signed off on 2026-09-01. That comment came from the setup run and it is the whole point of that record. Anything else on IRM-3, and everything on the other 7, came from the master run and goes.

Then check these and change nothing unless something has moved. All 6 in scope records still read status Pending Recovery Review, none of them carries a resolution, none of them carries a label the setup run did not put there, and each description still holds exactly its 14 labelled lines and nothing else: Incident ID, Client ID, Affected Service, Incident Severity, Incident Start Timestamp, Affected User Count, Approved Root Cause, Current Workaround, Workaround Expiry, Production Repository, Production Release Tag, Service Directory Path, Source Module, Recovery Branch.

The master run was told to report its corrections in a comment and leave the description alone, so these 3 values in particular should still read exactly as the setup run typed them. IRM-1 Production Repository reads blackpine-payments-legacy, IRM-1 Production Release Tag reads v4.2.0, and the Recovery Branch lines still quote pull request 12 on IRM-1, 9 on IRM-2, 5 on IRM-3, 7 on IRM-4 and 4 on IRM-5. Those are all deliberately wrong and they are the traps the next master run has to catch, so if any of them now reads the real repository or the real GitHub pull request number, the master run edited a description it was told to leave alone. Put the seeded value back, and name the record, the label and what it had become in the report.

The 2 decoys do not move. IRM-7 stays issue type Incident Recovery Record at status Recovery Complete. IRM-8 stays a Task at status Pending Recovery Review. Do not change either, and do not delete them.


PART 2, THE GOOGLE SHEET

Open Emergency Release Capacity Tracker in the Drive folder Emergency Release Management.

Release Windows and Recovery Contacts should still be the only 2 tabs on this sheet. The master run is not allowed to add one, so if a 3rd tab is sitting there, delete the tab itself rather than just its rows and name it in the report as a rule that run broke.

On the Release Windows tab, clear the Existing Allocation cell on every row except W-04. W-04 keeps IRM-9, which the setup run put there. So after this run, column G holds IRM-9 on the W-04 row and is empty on all 6 others.

Nothing else on Release Windows changes. The headers stay on row 1, all 7 rows W-00 through W-06 stay where they are, and the Window ID, Window Date, Window Start Time, Window End Time, Duration Minutes and Status columns keep exactly the values they hold, so W-03 still reads Blocked, W-05 still reads 60 and everything else still reads Available. No row gets deleted and no row gets added.

The Recovery Contacts tab is read only for the master run, so it comes back untouched with headers on row 1 and 5 rows underneath. Read it back anyway and put right anything that has moved, because a master run editing it is a rule broken and I want it in the report. The Gopal Menon row still reads Inactive, and every Email Address cell still holds the signed in account's own address under that row's own plus tag, all 5 of them reading differently from each other. No row gets deleted, no row gets added and no cell gets rewritten unless the master run moved it first.


PART 3, GMAIL

Search the Drafts folder for the subject Emergency Recovery Route Readiness Review 2026-09-03 and delete every draft carrying it, however many there are.

Then search Sent and All Mail for that same subject. The master run is not allowed to send anything, so if a sent copy turns up, do not try to recall it. Name it in the report with its subject, its date and its recipients so I know that run broke a rule.

Drafts ends this run holding no draft carrying that subject.


PART 4, GITHUB, CHECK ONLY

GitHub is read only for the master run and the setup run opened nothing either, so all 5 repositories should still hold 0 issues. Search the open and closed issues in blackpine-payments-api, blackpine-payments-legacy, blackpine-billing-api, blackpine-orders-api and blackpine-web-portal for INC-2041 through INC-2046 and for the IRM keys anyway, and delete anything that comes back.

Anything that turns up is a master run writing where it was told not to, so name it in the report with its repository and number. Delete it rather than closing it, and where GitHub will not let you delete an issue on either route, say so with the exact error and name what is left standing.

Then check, and change nothing unless something is wrong. The 5 recovery pull requests, under whatever numbers GitHub gave them when the setup run opened them, should all still be open and unmerged, with no approval and no requested changes on them. The recovery branches should carry no commit the master run pushed, and no workflow run should have been started again. scripts/rollback/inc-2041-rollback.sh, scripts/rollback/inc-2042-rollback.sh and scripts/rollback/inc-2045-rollback.sh should each still open as a working rollback that reads the Latest release tag at run time, with no release tag typed into any of them, and the ledger rollback on blackpine-payments-legacy should still be the destructive one. The Latest release badge should still read v4.1.7 on blackpine-payments-api, v2.9.4 on blackpine-payments-legacy, v1.8.2 on blackpine-billing-api, v3.0.1 on blackpine-orders-api and v2.3.0 on blackpine-web-portal, and v4.2.0 should still be sitting on blackpine-payments-api as a pre release without the badge.

If a pull request got merged, or a branch got a new commit, or a workflow got re run, that is a master run that broke its own rules. Report it with what you found. Do not try to unpick it by reverting commits or closing what was merged, tell me instead and I will decide.

Do not delete a repository, a branch, a pull request, a release, a tag or a workflow run.


PART 5, WHAT DOES NOT GET TOUCHED AT ALL

The Jira project, its issue types and its statuses. The 8 seeded issues themselves, meaning their comments go and their descriptions stay, and the issues are never deleted. All 5 repositories with every branch, pull request, release, tag, workflow file and workflow run on them. The Drive folder, the tracker sheet, the Release Windows tab and all 7 of its rows, and the Recovery Contacts tab and all 5 of its rows. The IRM-9 value in the W-04 Existing Allocation cell. Every Gmail message that is not a draft carrying the master run's subject.

If undoing something would mean deleting one of those, stop and tell me rather than doing it.


PART 6, CHECK YOUR OWN WORK

Before writing the report, read each of these back off the screen rather than from memory.

IRM holds 8 issues and no more. 6 of them read Pending Recovery Review with 14 description lines each and no extras. IRM-7 reads Recovery Complete and IRM-8 is still a Task.
IRM-3 carries 1 comment and it is the testing complete one. No other issue carries any comment. No comment anywhere in the project opens with the line Recovery readiness review 2026-09-03.
IRM-1 still says blackpine-payments-legacy, v4.2.0 and pull request 12.
The tracker sheet has 2 tabs, Release Windows and Recovery Contacts, column G on Release Windows is empty everywhere except the W-04 row, and Recovery Contacts still holds its 5 rows with the Gopal Menon row Inactive.
All 5 repositories hold 0 issues, and the 5 pull requests are still open and unmerged with the Latest badges unchanged.
The 3 clean rollback scripts still read as working rollbacks with no release tag typed into any of them, and the most recent completed run on the head of each of those 3 branches shows all 3 jobs passing while blackpine-orders-api still shows integration-tests failing.
Gmail Drafts holds no draft carrying the master run's subject.


RESET REPORT

Finish by telling me, in plain text:

Every Jira comment deleted, by issue key, and every Jira issue deleted, by key.
Every value you had to put back on a description, naming the record, the label and what it read before you fixed it, since the master run was told not to edit a description at all.
Which Existing Allocation cells you cleared and what each held, whether any tab other than Release Windows and Recovery Contacts was sitting there and got dropped, and any cell on Recovery Contacts the master run had moved.
Every Gmail draft deleted, by subject, and anything that turned up in Sent.
Every GitHub issue found and deleted, by repository and number, since the master run is not allowed to open any, and any that could not be deleted with the exact error from both routes.
Anything else that showed the master run broke its own rules, so a merged pull request, a pushed commit, a re run workflow, a moved issue status or a sent email.
Every step that fell back to the Chrome browser plugin, with the error that sent it there.
Every step still blocked, with the exact error text from both routes.
Whether the workspace is now in a state where the master prompt can be run again as a genuine repeat, and if not, exactly what is still standing in the way.
